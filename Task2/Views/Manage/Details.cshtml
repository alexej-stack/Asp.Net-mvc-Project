@model Task2.Models.Company
<link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
<script src="~/Scripts/ckeditor/ckeditor.js"></script>
<script src="~/Scripts/ckfinder/ckfinder.js"></script>
@{


    ViewBag.Title = Model.tName;
    var comments = (IEnumerable<Task2.Models.CompanyComment>)ViewBag.Comments;
    var ratingSum = ViewBag.RatingSum;
    var ratingCount = ViewBag.RatingCount;

    decimal rating = 0;
    if (ratingCount > 0)
    {
        rating = (ratingSum / ratingCount);
    }
    var totalRating = decimal.Parse(rating.ToString());
}
<div>
    <span class="starFadeN" id="sRate1"></span><span class="starFadeN" id="sRate2"></span><span class="starFadeN" id="sRate3"></span><span class="starFadeN" id="sRate4"></span><span class="starFadeN" id="sRate5"></span>
</div>
<!--<div class="tr" style="background-color:#f1f1f1;">
    <div class="td0">Please rate this school</div>
    <div class="td">Html.Partial("_VoteNow", Model.ID.ToString())</div>
    <div class="td">Html.Partial("_VoteShow", Model.Votes)</div>
</div>-->
<div>

    <hr />
    <dl class="dl-horizontal">


        <dd>
            @Html.Raw(Model.tName)
        </dd>



        <dd>
            @Html.Raw(Model.Description)
        </dd>



        <dd>
            @Html.Raw(Model.UserImage)
        </dd>
        
        <dd>
            @Html.Raw(Model.Video)
        </dd>



        <dd>
            @Html.DisplayFor(model => model.Tematic)
        </dd>

        <dd>
            @Html.Raw(Model.Galery)
        </dd>
    </dl>
</div>


<script type="text/javascript">

    $(function () {
        $('#UserImage').ckeditor();
    });
    $(function () {
        $('#tName').ckeditor();
    });
    $(function () {
        $('#Description').ckeditor();
    });
    $(function () {
        $('#Galery').ckeditor();
    });
    $(function () {
        $('#Video').ckeditor();
    });
</script>
<p>

    @Html.ActionLink("Back to List", "Index")
</p>
<input type="button" value="AddBonus" onclick="location.href='@Url.Action("ButtonAddBonus1", "Manage")'" />
@foreach (var c in comments)
{
    <hr />
    <div class="row">
        <div class="col-md-3">
            <i>@c.ThisDateTime</i>
            <br />
            @for (var i = 1; i <= c.Rating; i++)
            {
                <span class="starGlowN"></span>
            }
            @for (var i = (c.Rating + 1); i <= 5; i++)
            {
                <span class="starFadeN"></span>
            }
        </div>
        <div class="col-md-9">
            @Html.Raw(c.Comments.Replace("\n", "<br />"))


        </div>
    </div>
}
<hr />
@Html.Partial("_CommentBox")

<script>
    function SCRate() {
        for (var i = 1; i <= @totalRating; i++) {
            $("#sRate" + i).attr('class', 'starGlowN');
        }
    }
    $(function(){
        SCRate();
    });
</script>
